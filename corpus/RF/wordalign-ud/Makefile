# -*-makefile-*-
#

all:
	${MAKE} all-wordalign

include ../Makefile.def
include ../../Makefile.def

CORPUSXML        = ${CORPUSHOME}/parsed
CORPUSWORDALIGN  = ${OPUSWORDALIGN}/${CORPUS}/ud

include ../../Makefile.wordalign